// use clap::Parser;

/// Simple program to greet a person
// #[derive(Parser, Debug)]
// #[clap(author, version, about, long_about = None)]
// struct Args {
//     /// The name of the file to process
//     #[clap(short, long, value_parser)]
//     file: String,
//
//     // TODO: Deal Configuration
// }

fn main() {
    // let args = Args::parse();

    // Configure a deal for the file
}
