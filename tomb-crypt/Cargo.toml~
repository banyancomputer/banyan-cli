[package]
name = "tomb-crypt"
license = "LicenseRef-LICENSE.txt"
version = "0.1.0"
edition = "2021"

[dependencies]
base64 = { optional = true, version = "^0.21" }
# openssl = { optional = true, version = "^0.10", features = ["v110"] }
# openssl-hkdf = { optional = true, version = "^0.2" }
web-sys = { optional = true, version = "^0.3", features = [
    "Window",
    "Crypto",
    "CryptoKey",
    "CryptoKeyPair",
    "SubtleCrypto",
    "EcKeyGenParams",
]}
js-sys = { optional = true, version = "^0.3" }
wasm-bindgen = "0.2.87"
async-trait = "0.1.71"
wasm-bindgen-futures = "0.4.37"
gloo = { optional = true, version "0.8.1", features = [ "utils" ] }


[features]
# default = ["standard"]
# standard = ["dep:base64", "dep:openssl", "dep:openssl-hkdf"]
wasm = ["dep:web-sys", "dep:js-sys", "dep:base64"]

[dev-dependencies]
wasm-bindgen-test = "0.3.37"
